#Simple Bouncing Ball#
---

##Description##

A simple controllable ball that bounces off the edges of the screen (or nothing if the width is set smaller than the height). Includes both a Java (under src) and Javascript version.